projection implementation in class ZBP_C_05FLIGHT000 unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_05FLIGHT000 alias flight
extensible
use etag
{
//  use create;
  use update;
//
  field (readonly) PlaneTypeID;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}