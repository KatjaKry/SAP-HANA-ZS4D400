projection implementation in class ZBP_C_CS05_CUSTOMER unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_CS05_CUSTOMER alias CUSTOMER05
extensible
use etag
{
  use create;
  use update;
  use delete;

  field (readonly) City;  //City soll READONLY sein

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}